#include <stdio.h>

/**
 * wanna output?
 * command `gcc hello.c -o hello.out`
 * default output a.out
 */

int main ()
{
    /* if without \n, will display `hello world! %`, why? */
    // one line comments
    printf("hello world! hello, wplay! \n");
    return 0;
}